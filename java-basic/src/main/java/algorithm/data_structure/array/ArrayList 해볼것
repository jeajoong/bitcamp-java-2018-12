public Object[] toArray() {}
Object새로운 배열객체 list의 크기를 size만큼 만들고
list에 현재 배열 크기를 넣는다
그리고 list를 돌려받는다



public void add(Object value) {}
size크기와 배열 크기 비교후 같다면(배열을 늘려줘야하므로)
인수형 originSize에 현 배열크기를 넣고
newSize에 현 배열크기와 배열을 늘린값을 더해주고
Object배열 객체변수에 새로운 object배열을 새로운 사이즈까지 넣고
for문 돌리고 임시값 temp에 현배열크기를 넣었다
그리고 현 배열에 임시값 temp를 넣었다.
그리고 현 배열 사이즈를 늘린값에 value를 넣었다



public int insert(int index, Object value) {}
index 값 범위처리해주고 -1리턴
현재 사이즈가 현재 배열과 같을때 인크리지
삽입하는 과정에서 index 기준으로 오른쪽 값들은 오른쪽으로 한칸 이동되어야한다 반복하는 동안 주소+1에 주소를 넣어준다 
그리고 현재 배열 크기에 원하는 값을 넣고
배열 사이즈를 한칸 늘려준다. 


public Object get(int index) {}
index 범위값 설정해주고 현 배열에 index넣은값 리턴

public Object set(int index, Object value) {}
......

public Object remove(int index) {}
지울 방을 복사한다
index 부터 size -1 까지 배열을 땡겨주고
배열의 크기를 감소한다!



public int size() {
return this.size;
}



private void increase() {}




