13 -> 14 (클라이언트) DAO 프록시 패턴 적용하기

서버 쪽 dao 패키지를 복사해 붙여넣는다....


Agent 패키지를 proxy로 바꿔준다!
각Agent들을 각DaoProxy로 리펙터 해준다.

각DaoProxy에 각Dao를 implements로 상속 시켜준다 -> 형태가 달라진다... 
BoardDao의 메서드 선언한것과 같은 메소드명과 형태로 바꿔줘야한다.
그리고 원래 메소드 옆에서 예외처리를 던졌다면
try문이 끝날때 예외처리를 해주면 된다!


 - 핸들러 패키지
각 커맨드의 기능명령에서 DaoProxy를 사용하지 않고
복사해온 dao 인터페이스를 사용하게 된다

인터페이스 객체를 선언해주고 생성자에도 필드값으로 치환해준다

try 문에서 DaoProxy를 쓰지않고
이것 또한 Dao 인터페이스 객체의 메서드로 바꿔준다

